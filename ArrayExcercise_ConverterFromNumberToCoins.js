function converterFromNumberToCoins(number,arrayOfCoins) {

    console.log(arrayOfCoins.sort(function(a, b) { return a > b ? -1 : 1}));
    do while(number%element===0)
        const element = arrayOfCoins[index];
        if (number%element===0) {
            let result=0;
            result = number/element
            console.log("element".repeat(result))
        } else {
            

        }
    }
    

converterFromNumberToCoins(75,[25,10,5,1])